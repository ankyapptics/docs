---
title: "List orders"
description: "Get all orders for the authenticated account with filtering, sorting, and pagination"
api: "POST https://crm.apptics.ai/api/etc/api/public/v1/orders/list"
authMethod: "bearer"
---

## Documentation Index

Fetch the complete documentation index at: [https://docs.tagada.io/llms.txt](https://docs.tagada.io/llms.txt)
Use this file to discover all available pages before exploring further.

### Body Parameters

<ParamField body="search" type="string">
  Search query to filter orders.
</ParamField>

<ParamField body="sortBy" type="object">
  Object to define sorting logic.
  <Expandable title="properties">
    <ParamField body="field" type="string" required>
      The field to sort by. 
      Options: `payment.paymentInstrument.card.last4`, `summaries.totalAdjustedAmount`, `customer.name`, `customer.email`, `stores.name`, `orders.status`, `status`, `orders.createdAt`, `orders.draft`
    </ParamField>
    <ParamField body="direction" type="string" required>
      The direction of the sort. 
      Options: `asc`, `desc`
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="pagination" type="object" required>
  Pagination settings.
  <Expandable title="properties">
    <ParamField body="page" type="number" required>
      The page number to retrieve.
    </ParamField>
    <ParamField body="pageSize" type="number" required>
      The number of items per page.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="filters" type="object">
  Filtering criteria for the orders.
  <Expandable title="properties">
    <ParamField body="orders.status" type="string[]">
      Array of statuses to filter by.
    </ParamField>
    <ParamField body="customer.email" type="object">
      <Expandable title="properties">
        <ParamField body="condition" type="string">`contains` or `equals`</ParamField>
        <ParamField body="value" type="string">The email address or partial string.</ParamField>
      </Expandable>
    </ParamField>
    <ParamField body="orders.createdAt" type="object">
       <Expandable title="properties">
        <ParamField body="condition" type="string">`is-equal-date`, `is-between-date`, `is-after-date`, `is-before-date`</ParamField>
        <ParamField body="value" type="string[]">Array of dates.</ParamField>
      </Expandable>
    </ParamField>
  </Expandable>
</ParamField>

### Response

<ResponseField name="items" type="object[]">
  <Expandable title="Item Details">
    <ResponseField name="id" type="string">Order ID</ResponseField>
    <ResponseField name="status" type="string">Current status of the order</ResponseField>
    <ResponseField name="total" type="number">Total amount in cents</ResponseField>
    <ResponseField name="currency" type="string">ISO currency code</ResponseField>
  </Expandable>
</ResponseField>

<ResponseExample>

```json Basic Example
{
  "pagination": {
    "page": 1,
    "pageSize": 20
  }
}
```

</ResponseExample>